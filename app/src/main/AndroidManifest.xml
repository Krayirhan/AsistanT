<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <!-- CALL_PHONE - arama icin onceden izin gerektirir, DIAL ise gerekmez -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <!-- Rehber okuma (arama / WhatsApp yonlendirme) -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <!-- WiFi bilgisi -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- Kamera — fotoğraf çekme & el feneri (CameraManager.setTorchMode) -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Ses ayarları — medya tuş olayları gönderme (AudioManager.dispatchMediaKeyEvent) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Kamera ve flaş isteğe bağlı — cihazda bulunmayabilir -->
    <uses-feature android:name="android.hardware.camera"       android:required="false"/>
    <uses-feature android:name="android.hardware.camera.flash" android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AsistanT">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AsistanT">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.WakeWordService"
            android:foregroundServiceType="microphone"
            android:exported="false"/>

        <!-- WhatsApp otomatik gönderme — Erişilebilirlik servisi -->
        <service
            android:name=".services.WhatsAppAutoSendService"
            android:exported="true"
            android:label="Asistan WhatsApp Gönder"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/whatsapp_accessibility" />
        </service>
    </application>

</manifest>